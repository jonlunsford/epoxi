defmodule Epox.Mail.EncoderTest do
  use ExUnit.Case

  alias Epoxi.Mail.Encoder

  describe "handle_events" do
    test "it parses JSON strings" do
      #assert Encoder.handle_events([], 234, :no_state) == %Mailman.Email{}
    end
  end
end
